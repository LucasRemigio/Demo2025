// This file can be replaced during build by using the `fileReplacements` array.
// `ng serve --prod` replaces `environment.ts` with `environment.prod.ts`.
// `ng serve` replaces `environment.ts` with `environment.dev.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
    production: false,
    currrentBaseURL: '',
    refreshTimeMs: 15 * 1000, // x seconds
};
